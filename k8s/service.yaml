apiVersion: v1
kind: Service
metadata:
  name: portfolio
  namespace: portfolio-prod
  labels:
    environment: prod
spec:
  type: LoadBalancer
  selector:
    app: portfolio
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP